# Rust
/target
**/*.rs.bk
Cargo.lock.bak

# Built binaries
*.exe
*.pdb
*.dll

# Local data / runtime artifacts
*.db
*.db-journal
*.db-shm
*.db-wal
/config.local.toml
/downloads/
/logs/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# AI assistant workspace (планы диалогов и пр. служебные файлы)
.zcode/

# Release bundle (build-release.sh) — ~100MB, не в git
dist/

# Inno Setup build output (setup.exe — бинарный артефакт)
installer/Output/
